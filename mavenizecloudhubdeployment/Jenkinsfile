pipeline {

  agent any
  
  stages {
    stage('Build') {
      steps {
            bat 'mvn -B -U -e -V clean -DskipTests package'
      }
    }

     stage('Deployment') {
      steps {
           bat 'mvn clean package -Pdev deploy -DmuleDeploy'
      }
    }
    
  }
}